apiVersion: v1
kind: Service
metadata:
  name: ping-pong-svc
  namespace: log-output
spec:
  type: ClusterIP
  selector:
    app: ping-pong
  ports:
    - port: 5678
      protocol: TCP
      targetPort: 3002
